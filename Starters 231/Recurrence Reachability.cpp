#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define all(v) (v).begin(), (v).end()

void solve() {
    ll n,m;
    cin >>n>>m;
    if(m==1){
        cout<<-1<<endl;
        return;
    }
    if(n>=m){
        cout<<1<<" "<<m<<endl;
    }
    else{
        
    }


    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}